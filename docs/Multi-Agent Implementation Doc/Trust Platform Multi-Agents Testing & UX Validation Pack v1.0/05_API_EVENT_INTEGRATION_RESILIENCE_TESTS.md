# 05 — API, Event, Integration & Resilience Tests

Verify schemas, backward compatibility, canonical event envelopes, correlation IDs, auth context, timeouts, retries, circuit breakers, idempotency keys, outbox behavior and duplicate/out-of-order delivery. Contract-test PaymentGateway and any geolocation, notification, identity or AI provider adapters. Simulate latency, partial failure, malformed payloads and provider unavailability. No direct database/network bypass around approved gateways.
